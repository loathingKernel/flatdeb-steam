---
apt_components:
  - main
can_merge_usr: after_sysroot
has_automatic_dbgsym: true
strip_source_version_suffix: '\+srt[0-9]+'
sources:
  - label: steamrt
    apt_uri: 'http://repositories.internal.steamos.cloud/staging/steamrt-scout'
    keyring: 'valve-archive-keyring.gpg'
    apt_components:
      - main
  - label: steamrt-debug
    apt_uri: 'http://repositories.internal.steamos.cloud/staging/steamrt-scout'
    keyring: 'valve-archive-keyring.gpg'
    apt_components:
      - main/debug
    deb: true
    deb-src: false
  - label: steam
    for_build: false
    for_final: true
    apt_uri: 'http://repositories.internal.steamos.cloud/old-steamos-repo/steam'
    apt_suite: 'precise'
    keyring: 'valve-steam-keyring.gpg'
    apt_components:
      - steam
  - label: ubuntu
    for_build: false
    for_final: true
    apt_uri: 'http://archive.ubuntu.com/ubuntu'
    apt_suite: 'precise'
    keyring: 'ubuntu-archive-keyring.gpg'
    apt_components:
      - main
      - restricted
      - universe
      - multiverse
