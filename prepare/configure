#!/bin/sh
set -eu

ln -s . /app/app
ln -s . /app/usr
ln -s /var /app/var
cp -a /usr/var /
install -d /var/cache/apt/archives/partial

# We don't use the Makefile
cat > Makefile <<END
all:
	:

install:
	:
END
